package P5;

import android.util.SparseArray;

public enum z {
    NOT_SET,  // 此枚举字段已被移除
    EVENT_OVERRIDE;

    static {
        SparseArray sparseArray0 = new SparseArray();
        sparseArray0.put(0, z0);
        sparseArray0.put(5, z.a);
    }
}

