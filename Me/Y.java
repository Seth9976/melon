package Me;

import kotlin.jvm.internal.q;
import pe.b;

public enum y {
    FINAL,
    SEALED,
    OPEN,
    ABSTRACT;

    public static final O a;
    public static final y[] f;

    static {
        y.f = arr_y;
        q.g(arr_y, "entries");
        new b(arr_y);
        y.a = new O(5);
    }
}

