package sf;

import kotlin.jvm.internal.q;
import pe.b;

public enum o {
    PRETTY,  // 此枚举字段已被移除
    DEBUG,
    NONE;

    public static final o[] c;

    static {
        o.c = arr_o;
        q.g(arr_o, "entries");
        new b(arr_o);
    }
}

