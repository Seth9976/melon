package com.bumptech.glide.load;

public enum DataSource {
    LOCAL,
    REMOTE,
    DATA_DISK_CACHE,
    RESOURCE_DISK_CACHE,
    MEMORY_CACHE;

    private static DataSource[] $values() [...] // Inlined contents
}

