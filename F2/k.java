package F2;

import androidx.glance.appwidget.protobuf.z;

public enum k implements z {
    UNSPECIFIED_VERTICAL_ALIGNMENT(0),  // 此枚举字段已被移除
    TOP(1),
    CENTER_VERTICALLY(2),
    BOTTOM(3),
    UNRECOGNIZED(-1);

    public final int a;

    public k(int v1) {
        this.a = v1;
    }
}

