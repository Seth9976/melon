package F2;

import androidx.glance.appwidget.protobuf.z;

public enum j implements z {
    UNKNOWN_TYPE(0),  // 此枚举字段已被移除
    ROW(1),
    COLUMN(2),
    BOX(3),
    TEXT(4),
    LAZY_COLUMN(5),  // 此枚举字段已被移除
    LIST_ITEM(6),  // 此枚举字段已被移除
    CHECK_BOX(7),  // 此枚举字段已被移除
    BUTTON(8),  // 此枚举字段已被移除
    SPACER(9),
    SWITCH(10),  // 此枚举字段已被移除
    ANDROID_REMOTE_VIEWS(11),
    REMOTE_VIEWS_ROOT(12),
    IMAGE(13),
    LINEAR_PROGRESS_INDICATOR(14),
    CIRCULAR_PROGRESS_INDICATOR(15),  // 此枚举字段已被移除
    LAZY_VERTICAL_GRID(16),  // 此枚举字段已被移除
    VERTICAL_GRID_ITEM(17),  // 此枚举字段已被移除
    RADIO_GROUP(18),  // 此枚举字段已被移除
    RADIO_BUTTON(19),  // 此枚举字段已被移除
    RADIO_ROW(20),
    RADIO_COLUMN(21),
    SIZE_BOX(22),
    UNRECOGNIZED(-1);

    public final int a;

    public j(int v1) {
        this.a = v1;
    }
}

